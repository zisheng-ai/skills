# Static Template Asset

Keep this assets directory for optional starter templates, cover placeholders, or checked-in local fonts. Do not bundle large generated images or remote font files into the skill package.

For most tasks, generate the template directly in the target project instead of copying a fixed asset. This keeps the output aligned with the user's stack and language.
